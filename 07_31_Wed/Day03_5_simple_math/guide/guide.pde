// Use this example as a guide to go through mathematical functions.
// Use each step to alter the one sketch int mulitple sketches using "Save As..."

///////////////////////////////////////////////////////////// STEP 1
//int a = 30;
//line(a, 0, a, height);
//a = a + 40; // Assign 70 to a
//strokeWeight(4);
//line(a, 0, a, height);
//
/////////////////////////////////////////////////////////////// STEP 2
//int a = 30;
//int b = 40;
//line(a, 0, a, height);
//line(b, 0, b, height);
//strokeWeight(4);
//// A calculation can be used as an input to a function
//line(b-a, 0, b-a, height);
//
/////////////////////////////////////////////////////////////// STEP 3
//int a = 8;
//int b = 10;
//line(a, 0, a, height);
//line(b, 0, b, height);
//strokeWeight(4);
//line(a*b, 0, a*b, height);
//
/////////////////////////////////////////////////////////////// STEP 4
//int a = 8;
//int b = 10;
//line(a, 0, a, height);
//line(b, 0, b, height);
//strokeWeight(4);
//line(a/b, 0, a/b, height);
//
/////////////////////////////////////////////////////////////// STEP 5
//int y = 20;
//line(0, y, width, y);
//y = y + 6; // Assign 26 to y
//line(0, y, width, y);
//y = y + 6; // Assign 32 to y
//line(0, y, width, y);
//y = y + 6; // Assign 38 to y
//line(0, y, width, y);
//
/////////////////////////////////////////////////////////////// STEP 6
//float y = 20;
//line(0, y, width, y);
//y = y * 1.6; // Assign 32.0 to y
//line(0, y, width, y);
//y = y * 1.6; // Assign 51.2 to y
//line(0, y, width, y);
//y = y * 1.6; // Assign 81.920006 to y
//line(0, y, width, y);
