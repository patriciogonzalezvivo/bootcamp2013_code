void setup() {
  /*
  Let's use println() now to examine the different variable types
  */
  
  // Declare the variable x of type int
  // An integer is a whole number, Ex: 1, 2, 3, 4, 5, etc.
  int x;           
  
  // Declare the variable y of type float
  // A Float is a floating POINT number, 
  // meaning it is a number using a decimal point
  // Ex: 0.1, 0.5, 1.5, 3.14, 5.95, etc.
  float y;
  
  // Declare the variable s of type string
  // Strings are ASCII characters contained within " "
  // Ex: string s = "this can all be one string value"
  // Ex: string t = "also this 2 because it is inside quotation marks"
  String s;
  
  // Declare the variable b of type boolean
  // Booleans returns values that are either true or false
  // Its use is to see if an event has occurred
  // Ex: bool is_this_true = false;
  // This is more valuable when you start to implement logic into your code
  boolean b;
   
  // Assign the value 50 to x
  x = 50;          
  
  // Assign the value 12 to y, pay attention to how it looks in the console
  y = 12;
  
  // Assign the string value sasquatch to s
  s = "sasquatch";
  
  // Assign the value true to b
  b = true;

  println(x);
  println(y);
  println(s);
  println(b);
}

